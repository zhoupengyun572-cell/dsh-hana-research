# v27 可靠性与性能优化里程碑（2026-08-25）

## 目标

针对真实 Harness 巡检暴露的运行版本漂移、超大文献 DOM、同步刷新竞态、任务/笔记计数误导与装饰文字可访问性问题，完成第一轮可回归修复。

## 已完成

- 新增 `lib/version.js` 作为唯一发布版本源，页面资产与健康检查统一报告 `v27`。
- `package.json` 更新为 `0.2.7`，README 的数据库说明更新为 schema v13。
- 文献中心改为每批渲染 60 篇，并提供“继续显示”入口；筛选后自动回到首批。
- 1,408 篇真实文献的首屏 DOM 从约 49,068 个节点降到 2,375 个节点，按钮从约 13,677 个降到 664 个。
- 增加期刊同步后台状态监测，可识别页面打开后才启动、或在两次检查间完成的同步并刷新文献。
- “任务与笔记”页签改为任务数与项目笔记数合计，并用 title 拆分说明。
- 项目卡“打开项目”提示改为真实 HTML 装饰节点并设置 `aria-hidden`；标题装饰符号改为空内容视觉标记。
- 设置页明确 `Ctrl/⌘ K` 由 Harness 处理，并显示当前运行版本。
- Word/PDF 导出名称改为与实际行为一致：Word 兼容格式、打印/另存为 PDF。
- 新增大文献库分批渲染与任务/笔记合并计数回归断言。

## 验证

- 主项目：117/117 通过。
- 阅读工作区 Markdown：7/7 通过。
- JS 语法检查通过。
- 真实 Harness：首屏 60 张卡片；点击继续显示后为 120 张；关键词 `emotion` 得到 267 篇匹配且仅渲染 60 张。
- 真实项目：青少年情绪应对显示“任务与笔记 5”，说明为“0 个任务，5 条项目笔记”。
- 390×844 窄屏无横向溢出。
- 截图：`output/playwright/v27-literature-desktop.png`、`output/playwright/v27-project-mobile.png`。

## 部署与回滚

- 已同步 10 个生产文件到 `C:\Users\zhou\.dsh\profiles\node_modules\@local\dsh-hana-research`，SHA-256 全部一致。
- 可恢复备份：`C:\Users\zhou\.codex\backups\dsh-hana-research\20260825-201013`。
- 静态资源已由当前服务读到；宿主模块仍是旧进程，`releaseVersion: v27` 需要用户同意后重启 Harness 才会出现在健康检查中。

## 下一阶段

1. `/papers` 服务端分页、筛选与轻量 facets，进一步降低约 1.8 MB 的全量响应。
2. 同步状态改为显式事件/状态机，减少定时检查。
3. 生成真正的 DOCX/PDF/CSV/XLSX 文件。
4. 增加纳排标准、筛选状态、排除原因与自定义证据提取列。
5. Zotero/BibTeX/RIS 导入与重复文献合并。

