# HanaResearch v23 · Harness 原生内容层

## 已确认方向

- 视觉与结构以真实 DeepSeek Harness 为边界，不在插件内部复刻第二套应用顶栏。
- 项目列表与项目详情属于同一内容层；点击项目后替换列表，而不是打开固定遮罩抽屉。
- 高频内容保留在主区：下一步、最近活动、证据缺口；项目统计与 Agent 入口集中到右侧上下文。
- 低频工具（综述草稿、证据矩阵、导出、上传、项目设置）收进“更多”。

## 已完成实现

- `shell()` 改为 `native-page-head + app-content`，移除内部工作区切换、齿轮与命令按钮。
- 插件不再接管 Harness 的 `Ctrl/⌘ K`。
- 项目详情改为 `project-detail-view` 同层页面，返回时恢复项目列表与滚动位置。
- 详情页加入面包屑、状态、扁平页签、主内容/项目上下文双栏布局。
- 下一步卡取消折叠层；Agent 改为项目上下文中的安静次级操作。
- 动效仅保留 140ms 的轻微透明度/位移反馈，并支持 reduced-motion。
- 资源版本更新为 `v23`。

## 验证进度

- `node --check assets/research.js`：通过。
- `node --test tests/ui-interactions.test.mjs`：3/3 通过。
- 全量 `tests/*.test.mjs`：112/112 通过。
- `web/tests/markdown.test.mjs`：7/7 通过；合计 119/119。
- 运行副本已同步，源码与运行文件 SHA-256 一致。
- 恢复备份：`C:\Users\zhou\.codex\backups\dsh-hana-research\ui-v22-before-v23-sync-20260821-230000`。
- 未重启 Harness；通过外层“文献中心 / 项目库”切换完成静态资源热载入。

## 真实 Harness QA

- 外层只保留 Harness 自带的“文献中心 / 项目库 / 关闭 (Esc)”，插件内不再出现重复导航。
- 项目库列表在 1200×753 与最大化窗口下均无横向溢出。
- 点击“青少年情绪应对”后同层进入项目详情，返回入口清晰。
- 详情页主内容与右侧项目上下文层级稳定，页签可切换。
- “更多”菜单能展开，切换页签后自动收起。
- 清除了旧抽屉遗留的亮色竖线与标题 `§` 前缀。
- 任务页移除重复 Agent 入口，只保留项目上下文中的项目级入口。

## 后续可选方向

- 文献中心仍保留较浓的“学术编辑台”排版，可在下一迭代进一步向 Harness 的无衬线、紧凑列表统一。
- 主按钮的高亮颜色由 Harness 主题令牌决定，后续可继续降低白色主按钮的视觉权重。
