# 2026-08-21 交互可靠性与局部更新里程碑（v20）

## 本轮目标

- 解决部分按钮点击后看似无响应、可重复触发的问题。
- 减少高频操作后的整页重渲染，降低闪烁与交互中断。
- 让异步操作的进行中、慢响应、成功和失败状态保持一致。

## 已完成

- 新增统一 `runButtonAction` 操作状态：动作级互斥、按钮禁用、`aria-busy`、3 秒慢操作提示、成功/失败收尾与错误提示。
- 阅读状态、优先级、收藏、PDF 导入、方法标签改为卡片局部更新；失败时回滚，不再整页刷新文献中心。
- 关系、期刊源、保存检索、引文保存、项目创建、任务、笔记、全文翻译接入统一操作状态。
- 修复搜索结果“保存并导入”路径被旧忙碌锁提前拦截、界面显示导入但后端未执行的问题。
- 操作动效只保留异步 spinner 与短暂结果状态，高频筛选和局部更新保持即时；兼容 `prefers-reduced-motion`。
- 补充交互测试：局部 DOM 身份保持、乐观更新、失败回滚、搜索结果真实导入、抽屉关闭/重开竞态。

## 核验记录

- `node --check assets/research.js`：通过。
- `node --test tests/ui-interactions.test.mjs`：2/2 通过。
- 全量 `tests/*.test.mjs` + `web/tests/markdown.test.mjs`：118/118 通过，0 失败、0 跳过。
- 静态资源版本已提升为 `v20`。
- 运行副本已同步至 `C:\Users\zhou\.dsh\profiles\node_modules\@local\dsh-hana-research`，三个发布文件 SHA-256 均与源码一致。
- 同步前备份：`C:\Users\zhou\.codex\backups\dsh-hana-research\ui-v20-before-sync-20260821-200953`。
- 未自动重启 Harness；需要在用户方便时手动重启后加载 v20。

## 设计取舍

- 高频、可逆操作直接更新结果，不添加装饰性位移动画。
- 网络写操作才显示进度状态；超过 3 秒再提示“仍在处理”，避免正常请求产生噪声。
- 失败反馈保留在原位置并回滚数据，避免用户误以为操作已经成功。
